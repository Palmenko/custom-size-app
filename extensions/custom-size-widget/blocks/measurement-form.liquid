{% comment %}
  Форма мерок для товара
  Этот блок интегрируется с селектором размеров и показывает форму мерок только при выборе "custom size"
{% endcomment %}

{% assign app_url = shop.metafields.custom_size_app.app_url | default: 'http://localhost:62283' %}

<div class="custom-size-container" 
     data-app-url="{{ app_url }}"
     data-shop-url="{{ shop.url }}" 
     data-product-id="{{ product.id }}"
     style="display: none;">
  
  <div class="custom-size-form">
    <h3 class="custom-size-title">Ваши мерки для custom size</h3>
    <div class="custom-size-loading" style="display: block;">
    <p>Загрузка мерок...</p>
  </div>
    <div class="custom-size-content" style="display: none;">
      <div class="custom-size-fields"></div>
      <div class="custom-size-error" style="display: none;"></div>
    </div>
  </div>

  <style>
    .custom-size-container {
      margin: 20px 0;
      padding: 20px;
      border: 1px solid #e1e1e1;
      border-radius: 8px;
      background: #f9f9f9;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    }
    .custom-size-title {
      margin: 0 0 20px 0;
      color: #333;
      font-size: 18px;
      font-weight: 600;
    }
    .custom-size-fields {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }
    .custom-size-field {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }
    .custom-size-field label {
      font-weight: 600;
      color: #333;
      font-size: 14px;
      display: flex;
      align-items: center;
    }
    .custom-size-required {
      color: #d82c0d;
      margin-left: 4px;
    }
    .custom-size-description {
      font-size: 12px;
      color: #666;
      margin: 0;
    }
    .custom-size-input-group {
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .custom-size-input-group input {
      flex: 1;
      padding: 8px 12px;
      border: 1px solid #ccc;
      border-radius: 4px;
      font-size: 14px;
      font-family: inherit;
    }
    .custom-size-input-group input:focus {
      outline: none;
      border-color: #007cba;
      box-shadow: 0 0 0 2px rgba(0, 124, 186, 0.2);
    }
    .custom-size-input-group input.error {
      border-color: #d82c0d;
      background-color: #fff5f5;
    }
    .custom-size-input-group input.required-empty {
      border-color: #ffa500;
      background-color: #fff8e1;
    }
    .custom-size-unit {
      color: #666;
      font-size: 14px;
      font-weight: 500;
    }
    .custom-size-error {
      color: #d82c0d;
      font-size: 12px;
      margin: 4px 0 0 0;
      display: none;
    }
    .custom-size-range {
      font-size: 12px;
      color: #666;
      margin: 4px 0 0 0;
    }
    .custom-size-loading {
      text-align: center;
      padding: 20px;
      color: #666;
    }
    .custom-size-error {
      color: #d82c0d;
      text-align: center;
      padding: 20px;
      background: #fff5f5;
      border-radius: 4px;
      border: 1px solid #ffcdd2;
    }
    

  </style>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const container = document.querySelector('.custom-size-container');
      if (!container) return;
      
      const loadingEl = container.querySelector('.custom-size-loading');
      const contentEl = container.querySelector('.custom-size-content');
      const errorEl = container.querySelector('.custom-size-error');
      const fieldsEl = container.querySelector('.custom-size-fields');
      
      if (!loadingEl || !contentEl || !errorEl || !fieldsEl) {
        console.error('Не найдены необходимые элементы формы мерок');
        return;
      }
      
      const shopUrl = container.dataset.shopUrl;
      const productId = container.dataset.productId;
      const appUrl = container.dataset.appUrl;
      let measurements = [];
      let formData = {};
      
      // Делаем formData глобальным для доступа из других функций
      
      
      async function loadMeasurements() {
        try {
          console.log('Загружаем мерки...');
          
          // Используем динамический URL
          let appUrl = container.dataset.appUrl;
          if (appUrl && appUrl.includes('localhost')) {
            // Автоматически определяем правильный порт
            const currentPort = window.location.port || '80';
            appUrl = appUrl.replace(/localhost:\d+/, `localhost:${currentPort}`);
            console.log('🔧 Исправляем URL на правильный порт:', appUrl);
          }
          const shopUrl = container.dataset.shopUrl;
          const productId = container.dataset.productId;
          
          if (!appUrl || !shopUrl || !productId) {
            console.error('Отсутствуют необходимые данные:', { appUrl, shopUrl, productId });
            return;
          }
          
          console.log('Данные для загрузки мерок:', { appUrl, shopUrl, productId });
          
          // Получаем характеристики товара
          const productInfo = getProductInfo();
          console.log('Информация о товаре:', productInfo);
          
          // Добавляем отладочную информацию
          console.log('=== ОТЛАДКА ===');
          console.log('appUrl:', appUrl);
          console.log('shopUrl:', shopUrl);
          console.log('productId:', productId);
          console.log('productInfo:', productInfo);
          console.log('================');
          
          // Проверяем, есть ли теги
          if (!productInfo.tags) {
            console.log('⚠️ ВНИМАНИЕ: Теги товара не найдены!');
            console.log('Проверьте, что товар имеет тег "тест" в админке Shopify');
            
            // Пробуем найти теги в других местах
            console.log('🔍 Ищем теги в других местах...');
            
            // Ищем в data-атрибутах
            const dataTags = document.querySelector('[data-tags]');
            if (dataTags) {
              productInfo.tags = dataTags.getAttribute('data-tags');
              console.log('Теги найдены в data-tags:', productInfo.tags);
            }
            
            // Ищем в тексте страницы
            const pageText = document.body.textContent;
            const tagMatch = pageText.match(/тег[а-я]*:\s*([а-яё\s,]+)/i);
            if (tagMatch) {
              productInfo.tags = tagMatch[1].trim();
              console.log('Теги найдены в тексте страницы:', productInfo.tags);
            }
            
            // Если все еще нет тегов, добавляем тест
            if (!productInfo.tags) {
              console.log('🔧 ДОБАВЛЯЕМ ТЕГ "тест" ДЛЯ ТЕСТИРОВАНИЯ');
              productInfo.tags = 'тест';
            }
          } else {
            console.log('✅ Теги товара найдены:', productInfo.tags);
          }
          
          // Формируем URL с параметрами товара
          const params = new URLSearchParams({
            productId: productId,
            ...productInfo
          });
          
          const response = await fetch(`${appUrl}/api/product-measurements-groups?${params}`, {
            method: 'GET',
            headers: {
              'Content-Type': 'application/json',
              'Origin': shopUrl
            }
          });
          
          if (!response.ok) {
            throw new Error(`HTTP ${response.status}: ${response.statusText}`);
          }
          
          const contentType = response.headers.get('content-type');
          if (!contentType || !contentType.includes('application/json')) {
const text = await response.text();
            console.error('Получен не-JSON ответ:', text);
            throw new Error('Сервер вернул не-JSON ответ');
          }
          
          const data = await response.json();

          if (data.error) {
            throw new Error(data.error);
          }
          
          measurements = data.measurements || [];
          console.log('Загружены мерки для группы:', data.groupName, measurements.length);
          renderForm();
        } catch (error) {
          console.error('Ошибка загрузки мерок:', error);
          showError('Ошибка загрузки мерок: ' + error.message);
        }
      }
      
      function renderForm() {
        if (measurements.length === 0) {
          showEmpty();
          return;
        }
        
        // Инициализируем пустые значения для всех мерок
        measurements.forEach(measurement => {
          if (!formData[measurement.id]) {
            formData[measurement.id] = '';
          }
        });
        
        fieldsEl.innerHTML = measurements.map(measurement => {
          const value = formData[measurement.id] || '';
          const validationError = validateMeasurement(measurement, value);
          const isRequired = measurement.required;
          const isFilled = value !== '' && value !== null && value !== undefined;
          
          return `
            <div class="custom-size-field" data-measurement-id="${measurement.id}">
              <label for="custom-size-${measurement.id}">
                ${measurement.name}
                ${isRequired ? '<span class="custom-size-required">*</span>' : ''}
              </label>
              ${measurement.description ? `<p class="custom-size-description">${measurement.description}</p>` : ''}
              <div class="custom-size-input-group">
                <input
                  id="custom-size-${measurement.id}"
                  type="number"
                  min="${measurement.minValue}"
                  max="${measurement.maxValue}"
                  step="${measurement.step}"
                  value="${value}"
                  placeholder="Введите значение"
                  ${isRequired ? 'required' : ''}
                  class="${validationError ? 'error' : ''} ${isRequired && !isFilled ? 'required-empty' : ''}"
                  data-measurement-id="${measurement.id}"
                  data-required="${isRequired}"
                />
                <span class="custom-size-unit">${measurement.unit}</span>
              </div>
              ${validationError ? `<p class="custom-size-error">${validationError}</p>` : ''}
              ${isRequired && !isFilled ? `<p class="custom-size-error">Это поле обязательно для заполнения</p>` : ''}
              <p class="custom-size-range">
                Диапазон: ${measurement.minValue} - ${measurement.maxValue} ${measurement.unit}
              </p>
            </div>
          `;
        }).join('');
        
        addEventListeners();
        showContent();
      }
      
      function addEventListeners() {
        const inputs = fieldsEl.querySelectorAll('input[type="number"]');
        inputs.forEach(input => {
          if (input) {
          input.addEventListener('input', handleInputChange);
          input.addEventListener('blur', handleInputBlur);
          }
        });
      }
      
      function validateMeasurement(measurement, value) {
        if (measurement.required && (!value || value === '')) {
          return 'Это поле обязательно для заполнения';
        }
        
        if (value && value !== '') {
          const numValue = parseInt(value);
          if (isNaN(numValue)) {
            return 'Введите числовое значение';
          }
          
          if (numValue < measurement.minValue) {
            return `Значение должно быть не менее ${measurement.minValue} ${measurement.unit}`;
          }
          
          if (numValue > measurement.maxValue) {
            return `Значение должно быть не более ${measurement.maxValue} ${measurement.unit}`;
          }
        }
        
        return null;
      }
      
      function handleInputChange(event) {
        const measurementId = parseInt(event.target.dataset.measurementId);
        const value = event.target.value;
        
        console.log(`handleInputChange: measurementId=${measurementId}, value=${value}`);
        formData[measurementId] = value;
        console.log('formData после изменения:', formData);
        
        const measurement = measurements.find(m => m.id === measurementId);
        if (measurement) {
          const validationError = validateMeasurement(measurement, value);
          const errorEl = event.target.parentElement.parentElement.querySelector('.custom-size-error');
          
          // Обновляем классы поля
          event.target.classList.remove('error', 'required-empty');
          
          if (validationError) {
            event.target.classList.add('error');
            if (errorEl) {
              errorEl.textContent = validationError;
              errorEl.style.display = 'block';
            }
          } else {
            if (errorEl) {
              errorEl.style.display = 'none';
            }
          }
          
          // Проверяем, заполнено ли обязательное поле
          const isRequired = measurement.required;
          const isFilled = value !== '' && value !== null && value !== undefined && !isNaN(parseInt(value));
          
          if (isRequired && !isFilled) {
            event.target.classList.add('required-empty');
          }
        }
        
        // Добавляем мерки к форме при изменении
        setTimeout(() => {
          addMeasurementsToForm();
        }, 100);
      }
      
      function handleInputBlur(event) {
        const measurementId = parseInt(event.target.dataset.measurementId);
        const measurement = measurements.find(m => m.id === measurementId);
        if (measurement) {
          let value = event.target.value;
          console.log(`handleInputBlur: measurementId=${measurementId}, value=${value}`);
          
          // Если поле пустое и не обязательное, оставляем пустым
          if (!measurement.required && (value === '' || value === null || value === undefined)) {
            formData[measurementId] = '';
            return;
          }
          
          // Если поле пустое и обязательное, показываем ошибку
          if (measurement.required && (value === '' || value === null || value === undefined)) {
            formData[measurementId] = '';
            return;
          }
          
          let numValue = parseInt(value);
          if (!numValue || isNaN(numValue)) {
            if (measurement.required) {
              formData[measurementId] = '';
              return;
            } else {
              formData[measurementId] = '';
              return;
            }
          }
          
          if (numValue < measurement.minValue) {
            numValue = measurement.minValue;
            event.target.value = numValue;
            formData[measurementId] = numValue;
          } else if (numValue > measurement.maxValue) {
            numValue = measurement.maxValue;
            event.target.value = numValue;
            formData[measurementId] = numValue;
          } else {
            formData[measurementId] = numValue;
          }
          
          // Добавляем мерки к форме при потере фокуса
          setTimeout(() => {
            addMeasurementsToForm();
          }, 100);
        }
      }
      
      function showContent() {
        loadingEl.style.display = 'none';
        contentEl.style.display = 'block';
        errorEl.style.display = 'none';
      }
      
      function showError(message) {
        loadingEl.style.display = 'none';
        contentEl.style.display = 'none';
        errorEl.textContent = message;
        errorEl.style.display = 'block';
      }
      
      function showEmpty() {
        loadingEl.style.display = 'none';
        contentEl.style.display = 'block';
        errorEl.style.display = 'none';
        fieldsEl.innerHTML = '<div class="custom-size-empty">Мерки не настроены</div>';
      }
      
      // Функция для добавления мерок к форме товара
      function addMeasurementsToForm() {
        try {
          // Ищем форму товара с расширенным поиском
          let productForm = null;
          const formSelectors = [
            'form[action*="/cart/add"]',
            '.product-form',
            'form[name="product-form"]',
            'form[action*="/cart"]'
          ];
          
          for (const selector of formSelectors) {
            const forms = document.querySelectorAll(selector);
            for (const form of forms) {
              // Проверяем, что это форма товара (содержит поля товара)
              const hasProductFields = form.querySelector('input[name="id"], input[name="product_id"], select[name="id"]');
              if (hasProductFields) {
                productForm = form;
                break;
              }
            }
            if (productForm) break;
          }
          
          if (!productForm) {
            console.log('Форма товара не найдена, пробуем найти любую форму');
            productForm = document.querySelector('form');
          }
          
          if (!productForm) {
            console.log('Форма не найдена вообще');
            return;
          }
          
          console.log('Найдена форма товара:', productForm);
          console.log('Добавляем мерки к форме товара');
          console.log('formData:', formData);
          console.log('measurements:', measurements);
          
          // Удаляем старые поля мерок
          const oldMeasurementFields = productForm.querySelectorAll('input[name^="properties["]');
          oldMeasurementFields.forEach(field => {
            if (field && field.parentNode && (field.name.includes('measurements') || field.name.includes('Рост') || field.name.includes('Обхват') || field.name.includes('Длина'))) {
              field.parentNode.removeChild(field);
            }
          });
          
          // Добавляем новые поля мерок
          let addedCount = 0;
          Object.keys(formData).forEach(measurementId => {
            const value = formData[measurementId];
            console.log(`Проверяем мерку ${measurementId}:`, value);
            if (value !== '' && value !== null && value !== undefined) {
              const measurement = measurements.find(m => m.id === parseInt(measurementId));
              console.log('Найдена мерка:', measurement);
              if (measurement) {
                // Добавляем мерки в формате properties для Shopify
                const input = document.createElement('input');
                input.type = 'hidden';
                input.name = `properties[${measurement.name}]`;
                input.value = `${value} ${measurement.unit}`;
                productForm.appendChild(input);
                addedCount++;
                console.log(`Добавлено поле: ${measurement.name} = ${value} ${measurement.unit}`);
                
                // Также добавляем в старом формате для совместимости
                const inputOld = document.createElement('input');
                inputOld.type = 'hidden';
                inputOld.name = `properties[measurements][${measurement.name}]`;
                inputOld.value = `${value} ${measurement.unit}`;
                productForm.appendChild(inputOld);
                console.log(`Добавлено поле (старый формат): measurements[${measurement.name}] = ${value} ${measurement.unit}`);
              }
            }
          });
          
          console.log(`Всего добавлено полей мерок: ${addedCount}`);
          
          // Проверяем, что поля действительно добавлены
          const newMeasurementFields = productForm.querySelectorAll('input[name^="properties["]');
          console.log('Поля мерок в форме:', newMeasurementFields.length);
          newMeasurementFields.forEach(field => {
            console.log(`Поле: ${field.name} = ${field.value}`);
          });
          
        } catch (error) {
          console.error('Ошибка при добавлении мерок к форме:', error);
        }
      }
      
      // Функция для получения информации о товаре
      function getProductInfo() {
        const info = {};
        
        console.log('=== getProductInfo ===');
        
        try {
          // 1. Пробуем получить теги из meta тегов
          const productTags = document.querySelector('meta[property="product:tag"]');
          console.log('productTags meta:', productTags);
          if (productTags) {
            info.tags = productTags.getAttribute('content');
            console.log('Теги из meta:', info.tags);
          }
          
          // 2. Пробуем получить теги из data-product-json
          const productJson = document.querySelector('script[data-product-json]');
          console.log('productJson script:', productJson);
          if (productJson) {
            try {
              const productData = JSON.parse(productJson.textContent);
              console.log('productData:', productData);
              if (productData.tags) {
                info.tags = productData.tags;
                console.log('Теги из product JSON:', info.tags);
              }
              if (productData.product_type) {
                info.type = productData.product_type;
                console.log('Тип из product JSON:', info.type);
              }
              if (productData.vendor) {
                info.vendor = productData.vendor;
                console.log('Вендор из product JSON:', info.vendor);
              }
            } catch (e) {
              console.log('Ошибка парсинга product JSON:', e);
            }
          }
          
          // 3. Пробуем получить теги из всех возможных скриптов
          const allScripts = document.querySelectorAll('script');
          console.log('Всего скриптов на странице:', allScripts.length);
          
          for (let i = 0; i < allScripts.length; i++) {
            const script = allScripts[i];
            const content = script.textContent || script.innerHTML;
            
            // Ищем скрипты с данными о товаре
            if (content.includes('"tags"') || content.includes('"product"')) {
              console.log(`Скрипт ${i} содержит данные о товаре:`, script);
              try {
                // Пробуем найти JSON в скрипте
                const jsonMatch = content.match(/\{[\s\S]*\}/);
                if (jsonMatch) {
                  const jsonData = JSON.parse(jsonMatch[0]);
                  console.log(`JSON из скрипта ${i}:`, jsonData);
                  
                  if (jsonData.tags && !info.tags) {
                    info.tags = jsonData.tags;
                    console.log('Теги найдены в скрипте:', info.tags);
                  }
                  
                  if (jsonData.product && jsonData.product.tags && !info.tags) {
                    info.tags = jsonData.product.tags;
                    console.log('Теги найдены в product:', info.tags);
                  }
                }
              } catch (e) {
                console.log(`Ошибка парсинга скрипта ${i}:`, e);
              }
            }
          }
          
          // 4. Пробуем получить теги из JSON-LD
          const jsonLdScripts = document.querySelectorAll('script[type="application/ld+json"]');
          console.log('JSON-LD скрипты:', jsonLdScripts.length);
          jsonLdScripts.forEach((script, index) => {
            try {
              const data = JSON.parse(script.textContent);
              console.log(`JSON-LD ${index}:`, data);
              
              if (data['@type'] === 'Product') {
                if (data.category && !info.collections) {
                  info.collections = data.category;
                }
                if (data.description && data.description.includes('тег')) {
                  console.log('Найдено упоминание тега в описании:', data.description);
                }
              }
            } catch (e) {
              console.log(`JSON-LD ${index} parse error:`, e);
            }
          });
          
          // 5. Пробуем получить теги из meta тегов с разными вариантами
          const metaSelectors = [
            'meta[property="product:tag"]',
            'meta[name="product:tag"]',
            'meta[property*="tag"]',
            'meta[name*="tag"]'
          ];
          
          for (const selector of metaSelectors) {
            const metaTags = document.querySelectorAll(selector);
            console.log(`Meta теги с селектором "${selector}":`, metaTags.length);
            metaTags.forEach((meta, index) => {
              console.log(`Meta ${index}:`, meta.getAttribute('property') || meta.getAttribute('name'), '=', meta.getAttribute('content'));
              if (!info.tags && meta.getAttribute('content')) {
                info.tags = meta.getAttribute('content');
                console.log('Теги найдены в meta:', info.tags);
              }
            });
          }
          
          // 6. Пробуем получить теги из атрибутов data-*
          const dataElements = document.querySelectorAll('[data-tags], [data-product-tags]');
          console.log('Элементы с data-tags:', dataElements.length);
          dataElements.forEach((el, index) => {
            const tags = el.getAttribute('data-tags') || el.getAttribute('data-product-tags');
            console.log(`Data tags ${index}:`, tags);
            if (tags && !info.tags) {
              info.tags = tags;
              console.log('Теги найдены в data-атрибуте:', info.tags);
            }
          });
          
          // Получаем остальную информацию о товаре
          const productType = document.querySelector('meta[property="product:type"]');
          if (productType) {
            info.type = productType.getAttribute('content');
          }
          
          const productVendor = document.querySelector('meta[property="product:vendor"]');
          if (productVendor) {
            info.vendor = productVendor.getAttribute('content');
          }
          
          const productTemplate = document.querySelector('meta[name="template"]');
          if (productTemplate) {
            info.template = productTemplate.getAttribute('content');
          }
          
        } catch (error) {
          console.error('Ошибка при получении информации о товаре:', error);
        }
        
        console.log('Итоговая информация о товаре:', info);
        console.log('=== /getProductInfo ===');
        
        return info;
      }
      
      // Функция для инициализации формы мерок
      function initializeMeasurementForm() {
        try {
          console.log('Инициализация формы мерок');
          
          // Загружаем мерки
      loadMeasurements();
          
          // Настраиваем отслеживание изменений вариаций
          setupVariantChangeListener();
          
        } catch (error) {
          console.error('Ошибка при инициализации формы мерок:', error);
        }
      }
      
      // Функция для отслеживания изменений вариаций
      function setupVariantChangeListener() {
        try {
          console.log('Настраиваем отслеживание изменений вариаций');
          
          // Ищем все возможные селекторы вариаций
          const variantSelectors = [
            'select[name="id"]',
            'select[name="product_id"]',
            'select[name="variant_id"]',
            '.product-form select',
            'form[action*="/cart/add"] select',
            'input[name*="Size"][type="radio"]',
            'input[name*="size"][type="radio"]',
            'input[name*="Size-1"][type="radio"]',
            '.product-form__input--pill input[type="radio"]'
          ];
          
          let foundSelector = null;
          let selectorType = null;
          
          // Проверяем каждый селектор
          for (const selector of variantSelectors) {
            const elements = document.querySelectorAll(selector);
            if (elements.length > 0) {
              foundSelector = elements;
              selectorType = selector.includes('radio') ? 'radio' : 'select';
              console.log('Найден селектор вариаций:', selector, 'тип:', selectorType);
              break;
            }
          }
          
          if (!foundSelector) {
            console.log('Селектор вариаций не найден');
            return;
          }
          
          // Добавляем обработчики событий
          if (selectorType === 'select') {
            // Для select элементов
            foundSelector.forEach(select => {
              select.addEventListener('change', handleVariantChange);
              console.log('Добавлен обработчик change для select:', select);
            });
            
            // Проверяем текущее состояние
            foundSelector.forEach(select => {
              const selectedOption = select.options[select.selectedIndex];
              if (selectedOption) {
                const selectedValue = select.value;
                const selectedText = selectedOption.text;
                
                console.log('Текущая выбранная вариация:', selectedValue, 'текст:', selectedText);
                
                const customSizeKeywords = [
                  'custom', 'custom size', 'custom-size', 'customsize',
                  'индивидуальный', 'индивидуальный размер', 'индивидуальный размер',
                  'по мерке', 'по меркам', 'на заказ', 'заказной',
                  'custom size', 'custom-size', 'customsize'
                ];
                
                const selectedValueLower = selectedValue.toLowerCase();
                const selectedTextLower = selectedText.toLowerCase();
                
                const isCustomSize = customSizeKeywords.some(keyword => 
                  selectedValueLower.includes(keyword) || selectedTextLower.includes(keyword)
                );
                
                if (isCustomSize) {
                  container.style.display = 'block';
                  console.log('Форма мерок показана (текущее состояние)');
                }
              }
            });
          } else if (selectorType === 'radio') {
            // Для radio элементов
            foundSelector.forEach(radio => {
              radio.addEventListener('change', handleVariantChange);
              console.log('Добавлен обработчик change для radio:', radio);
            });
            
            // Проверяем текущее состояние
            const checkedRadio = Array.from(foundSelector).find(radio => radio.checked);
            if (checkedRadio) {
              const selectedValue = checkedRadio.value;
              console.log('Текущая выбранная вариация (radio):', selectedValue);
              
              const customSizeKeywords = [
                'custom', 'custom size', 'custom-size', 'customsize',
                'индивидуальный', 'индивидуальный размер', 'индивидуальный размер',
                'по мерке', 'по меркам', 'на заказ', 'заказной',
                'custom size', 'custom-size', 'customsize'
              ];
              
              const selectedValueLower = selectedValue.toLowerCase();
              
              const isCustomSize = customSizeKeywords.some(keyword => 
                selectedValueLower.includes(keyword)
              );
              
              if (isCustomSize) {
                container.style.display = 'block';
                console.log('Форма мерок показана (текущее состояние radio)');
              }
            }
          }
          
          // Добавляем MutationObserver для отслеживания динамических изменений
          const observer = new MutationObserver(function(mutations) {
            mutations.forEach(function(mutation) {
              if (mutation.type === 'childList' || mutation.type === 'attributes') {
                // Проверяем, изменились ли селекторы вариаций
                const variantElements = document.querySelectorAll('select[name="id"], select[name="product_id"], select[name="variant_id"], input[name*="Size"][type="radio"]');
                variantElements.forEach(element => {
                  if (element.value && !element.hasAttribute('data-custom-size-listener')) {
                    element.setAttribute('data-custom-size-listener', 'true');
                    element.addEventListener('change', handleVariantChange);
                    console.log('Добавлен обработчик для динамически созданного элемента:', element);
                  }
                });
              }
            });
          });
          
          // Начинаем наблюдение за изменениями в DOM
          observer.observe(document.body, {
            childList: true,
            subtree: true,
            attributes: true,
            attributeFilter: ['value', 'checked']
          });
          
        } catch (error) {
          console.error('Ошибка при настройке отслеживания вариаций:', error);
        }
      }
      
      // Обработчик изменения вариации
      function handleVariantChange(event) {
        try {
          const selectedValue = event.target.value;
          const selectedText = event.target.options ? event.target.options[event.target.selectedIndex]?.text : event.target.value;
          
          console.log('Изменена вариация:', selectedValue, 'текст:', selectedText);
          
          // Проверяем, является ли выбранная вариация "Custom size"
          const customSizeKeywords = [
            'custom', 'custom size', 'custom-size', 'customsize',
            'индивидуальный', 'индивидуальный размер', 'индивидуальный размер',
            'по мерке', 'по меркам', 'на заказ', 'заказной',
            'custom size', 'custom-size', 'customsize'
          ];
          
          const selectedValueLower = selectedValue.toLowerCase();
          const selectedTextLower = selectedText.toLowerCase();
          
          const isCustomSize = customSizeKeywords.some(keyword => 
            selectedValueLower.includes(keyword) || selectedTextLower.includes(keyword)
          );
          
          console.log('Это Custom size?', isCustomSize);
          
          if (isCustomSize) {
            // Показываем форму мерок
            container.style.display = 'block';
            console.log('Форма мерок показана');
          } else {
            // Скрываем форму мерок
            container.style.display = 'none';
            console.log('Форма мерок скрыта');
          }
          
        } catch (error) {
          console.error('Ошибка при обработке изменения вариации:', error);
        }
      }
      

      

      

      
      // Инициализация
      initializeMeasurementForm();
      
      // 🔧 ПРИНУДИТЕЛЬНО ПОКАЗЫВАЕМ ФОРМУ МЕРОК ДЛЯ ТЕСТИРОВАНИЯ
      console.log('🔧 ПРИНУДИТЕЛЬНО ПОКАЗЫВАЕМ ФОРМУ МЕРОК ДЛЯ ТЕСТИРОВАНИЯ');
      container.style.display = 'block';
    });
  </script>
</div> 

{% schema %}
{
  "name": "Custom Size Form",
  "target": "section",
  "settings": []
}
{% endschema %} 